server:
  port: 5002

spring:
  application:
    name: airline-core-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_db?useUnicode=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: qwer1234

  jpa:
    hibernate:
      ddl-auto: update
